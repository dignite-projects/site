{
  "packages":[
    {
      "appRoutingModuleConfiguration":{
        "imports":[
          {
            "names":[
              "SITE_ROUTES"
            ],
            "namespace": "@dignite/site"
          } 
        ],
        "routes":[
          "{ path: 'site', loadChildren: () => import('@dignite/site').then(c => c.SITE_ROUTES) }"
        ]
      },
      "appModuleConfiguration":{
        "imports":[
          {
            "names":[
              "provideSite"
            ],
            "namespace": "@dignite/site/config"
          }
        ],
        "providerNames":[
          "provideSite()"
        ]
      },
      "npmPackages":[
        {
          "name": "@dignite/ng.flex-fields",
          "note": "Not published to npm. Consumed as a file: dependency pointing at the sibling repo's built output; requires preserveSymlinks in tsconfig and angular.json so its @angular/* resolve to the host's copies."
        },
        {
          "name": "ng-zorro-antd",
          "note": "Peer dependency of @dignite/ng.flex-fields (its TreeView control uses nz-tree-select)."
        },
        {
          "name": "@angular/cdk",
          "note": "Peer dependency of @dignite/ng.flex-fields; also used by the content type field arrangement's drag and drop."
        }
      ],
      "tsJsonPathRecordConfigurations":[
        {
          "name": "@dignite/site",
          "paths": [
            "angular/projects/site/src/public-api.ts"
          ]
        },
        {
          "name": "@dignite/site/config",
          "paths": [
            "angular/projects/site/config/src/public-api.ts"
          ]
        }
      ]
    }
  ]
}